# Orbit Voice Background Probe

Standalone iOS experiment. It tests whether an `AudioRecordingIntent` can activate
`AVAudioSession` and receive microphone buffers without foregrounding the app.

This target has no Orbit backend, pairing, TTS, OpenRouter, memory, or Shortcut
integration. It is intentionally independent of every existing Orbit target.
